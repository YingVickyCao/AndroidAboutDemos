<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--
    We make the pressed and focused selector items transparent so the ListView's own selector states show through.
    -->
    <item android:drawable="@android:color/transparent" android:state_pressed="true" />
    <item android:drawable="@android:color/transparent" android:state_focused="true" />
    <item android:drawable="@color/message_list_item_background_unread" app:state_message_unread="true" />
</selector>