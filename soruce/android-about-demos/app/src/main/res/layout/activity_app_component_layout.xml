<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_horizontal">

    <TextView
        android:id="@+id/topic"
        style="@style/topic"
        android:text="@string/page_app_component" />

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/size_topic">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <Button
                android:id="@+id/pageBondService"
                style="@style/btn.small"
                android:text="@string/page_bond_service" />

            <Button
                android:id="@+id/pageBondService2"
                style="@style/btn.small"
                android:text="@string/page_bond_service" />

            <Button
                android:id="@+id/pageContentProvider"
                style="@style/btn.small"
                android:text="@string/page_content_provider" />

            <Button
                android:id="@+id/pageDialogFragment"
                style="@style/btn.small"
                android:text="@string/page_DialogFragment" />

        </LinearLayout>
    </ScrollView>

    <FrameLayout
        android:id="@+id/fragmentRoot"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</FrameLayout>